# Copyright (C) 2026 SymWorx

[package]
name = "symworx-loadsym-db"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
description = "Reusable SQL schema for personal training-load catalogs (Postgres/SQLite; schema v4 multi-source)"
documentation = "https://docs.rs/symworx-loadsym-db"
readme = "README.md"
keywords = ["database", "schema", "periodization", "training-load", "loadsym"]
categories = ["science"]

[lib]
path = "src/lib.rs"

[dependencies]
# Intentionally zero runtime dependencies. Pure resources + helpers.

[lints]
workspace = true
