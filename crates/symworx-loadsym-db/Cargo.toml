# Copyright (C) 2026 SymWorx

[package]
name = "symworx-loadsym-db"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
description = "Reusable SQL schema and helpers for initializing training load databases (Postgres/SQLite)"
keywords = ["database", "schema", "periodization", "training-load"]
categories = ["science"]

[lib]
path = "src/lib.rs"

[dependencies]
# Intentionally zero runtime dependencies. Pure resources + helpers.

[lints]
workspace = true
