[package]
name = "csymd-core"
version = "0.1.0"
edition = "2024"

[dependencies]
ndarray = { version = "0.15", features = ["std"] }
ndarray-linalg = { version = "0.16", features = ["openblas"] }
rand = "0.8"
